//Disclaimer Let e Var

var nome = "Luiz"; // var deve ser evitado pois pode redeclarar variavel e atrapalhar o código
var nome = "Ótavio"; // NÃO FAÇA ISSO

console.log(nome);

//Tipos Primitivos

const name = "Gica"; // String Type
const num1 = 10; //Number type
const num2 = 10.52; //Number type too
let nomeAluno; // undefined -> não aponta pra lugar nenhum da memória
let sobrenomeAluno = null; // nulo -> não aponta pra lugar nenhum da memória mas é setado pelo dev, diferente de nomeAluno
const aprovado = true; //boolean type = true, false (lógico)

console.log(typeof aprovado)